<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tocar"
        android:id="@+id/buttonTocar"
        android:layout_marginBottom="21dp"

        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Detener"
        android:id="@+id/buttonDetener"
        android:layout_alignBottom="@+id/buttonTocar"
        android:layout_toRightOf="@+id/buttonTocar"
        android:layout_toEndOf="@+id/buttonTocar"

        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mensaje"
        android:id="@+id/buttonMensaje"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/buttonDetener"
        android:layout_toEndOf="@+id/buttonDetener"

        />

</RelativeLayout>
